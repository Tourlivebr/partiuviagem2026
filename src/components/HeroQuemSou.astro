---
// Nenhuma lógica do servidor necessária
---

<section id="quem-sou" class="relative min-h-[70vh] md:min-h-[85vh] lg:min-h-screen flex items-center justify-center overflow-hidden pt-16 md:pt-18 lg:pt-20">
  <!-- Background Images Carousel -->
  <div class="absolute top-0 left-0 right-0 bottom-0 z-0">
    <!-- Imagem 1 -->
    <div 
      id="bg-image-1"
      class="absolute inset-0 bg-cover bg-center transition-opacity duration-1000 opacity-100"
      style="background-image: url('/images/carrossel/portico_gramado.jpg'); background-attachment: fixed;"
    ></div>
    
    <!-- Imagem 2 -->
    <div 
      id="bg-image-2"
      class="absolute inset-0 bg-cover bg-center transition-opacity duration-1000 opacity-0"
      style="background-image: url('/images/carrossel/lago_negro.jpg'); background-attachment: fixed;"
    ></div>
    
    <!-- Imagem 3 -->
    <div 
      id="bg-image-3"
      class="absolute inset-0 bg-cover bg-center transition-opacity duration-1000 opacity-0"
      style="background-image: url('/images/carrossel/fonte_amor.jpg'); background-attachment: fixed;"
    ></div>
    
    <!-- Overlay escuro para melhor legibilidade -->
    <div class="absolute inset-0 bg-gradient-to-t from-black/20 via-black/20 to-black/10"></div>
  </div>
  
  <!-- Conteúdo -->
  <div class="relative z-10 container mx-auto px-4 py-4 md:py-6 lg:py-8">
    <div class="max-w-4xl mx-auto text-center text-white">
      <!-- Título Principal -->
      <div class="space-y-2 md:space-y-3 mb-4 md:mb-5">
        <h1 class="text-3xl md:text-4xl lg:text-6xl xl:text-7xl font-bold leading-tight drop-shadow-2xl">
          Olá, bem vindo à <span class="text-primary-400 drop-shadow-lg">Serra Gaúcha</span>
        </h1>
        
        <p class="text-lg md:text-xl lg:text-2xl leading-relaxed max-w-3xl mx-auto text-white drop-shadow-lg font-medium bg-black/20 backdrop-blur-sm rounded-lg p-3 md:p-4 lg:p-6">
          A Partiu Viagem tem os melhores roteiros de Gramado e Canela para você viver momentos inesquecíveis com até 50% de economia.
        </p>
      </div>
      
      <!-- Botões de Ação -->
      <div class="flex flex-col sm:flex-row gap-2 md:gap-3 justify-center mb-4 md:mb-6">
        <a 
          href="#contato" 
          class="bg-primary-600 hover:bg-primary-700 text-white font-bold py-3 md:py-4 px-6 md:px-8 rounded-lg transition-all duration-300 flex items-center justify-center space-x-2 scroll-smooth shadow-2xl hover:shadow-3xl transform hover:scale-105"
        >
          <svg class="w-4 h-4 md:w-5 md:h-5" fill="currentColor" viewBox="0 0 24 24">
            <path d="M20 4H4c-1.1 0-1.99.9-1.99 2L2 18c0 1.1.9 2 2 2h16c1.1 0 2-.9 2-2V6c0-1.1-.9-2-2-2zm0 4l-8 5-8-5V6l8 5 8-5v2z"/>
          </svg>
          <span>Entre em contato</span>
        </a>
        
        <a 
          href="#passeios" 
          class="border-2 border-white bg-white/10 backdrop-blur-sm text-white hover:bg-white hover:text-secondary-900 font-bold py-3 md:py-4 px-6 md:px-8 rounded-lg transition-all duration-300 flex items-center justify-center space-x-2 scroll-smooth shadow-2xl hover:shadow-3xl transform hover:scale-105"
        >
          <svg class="w-4 h-4 md:w-5 md:h-5" fill="currentColor" viewBox="0 0 24 24">
            <path d="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z"/>
          </svg>
          <span>Veja os passeios</span>
        </a>
      </div>
      
      <!-- Estatísticas -->
      <div class="grid grid-cols-3 gap-3 md:gap-6 max-w-2xl mx-auto">
        <div class="text-center bg-white/20 backdrop-blur-md rounded-lg p-3 md:p-4 shadow-xl border border-white/20">
          <div class="text-2xl md:text-3xl lg:text-4xl font-bold text-primary-400 mb-1 md:mb-2 drop-shadow-lg">17000+</div>
          <div class="text-xs md:text-sm text-white font-medium drop-shadow-md">Clientes Satisfeitos</div>
        </div>
        <div class="text-center bg-white/20 backdrop-blur-md rounded-lg p-3 md:p-4 shadow-xl border border-white/20">
          <div class="text-2xl md:text-3xl lg:text-4xl font-bold text-primary-400 mb-1 md:mb-2 drop-shadow-lg">9</div>
          <div class="text-xs md:text-sm text-white font-medium drop-shadow-md">Anos de Experiência</div>
        </div>
        <div class="text-center bg-white/20 backdrop-blur-md rounded-lg p-3 md:p-4 shadow-xl border border-white/20">
          <div class="text-2xl md:text-3xl lg:text-4xl font-bold text-primary-400 mb-1 md:mb-2 drop-shadow-lg">4.9</div>
          <div class="text-xs md:text-sm text-white font-medium drop-shadow-md">Avaliação Média</div>
        </div>
      </div>
    </div>
  </div>
  
  <!-- Seta para baixo -->
  <div class="absolute bottom-4 md:bottom-8 left-1/2 transform -translate-x-1/2 z-10">
    <a href="#passeios" class="text-white hover:text-primary-400 transition-colors animate-bounce drop-shadow-lg">
      <svg class="w-6 h-6 md:w-8 md:h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 14l-7 7m0 0l-7-7m7 7V3"></path>
      </svg>
    </a>
  </div>
</section>

<script>
  // Carrossel de imagens de fundo
  let currentImageIndex = 0;
  const images = ['bg-image-1', 'bg-image-2', 'bg-image-3'];
  
  function changeBackgroundImage() {
    // Ocultar imagem atual
    const currentImage = document.getElementById(images[currentImageIndex]);
    if (currentImage) {
      currentImage.style.opacity = '0';
    }
    
    // Próxima imagem
    currentImageIndex = (currentImageIndex + 1) % images.length;
    
    // Mostrar próxima imagem
    const nextImage = document.getElementById(images[currentImageIndex]);
    if (nextImage) {
      nextImage.style.opacity = '1';
    }
  }
  
  // Iniciar carrossel automático
  setInterval(changeBackgroundImage, 5000); // Muda a cada 5 segundos
</script>

<style>
  @keyframes zoom-in {
    0% {
      transform: scale(1.2);
      opacity: 0;
    }
    100% {
      transform: scale(1);
      opacity: 1;
    }
  }
  
  .animate-zoom-in {
    animation: zoom-in 2s ease-out forwards;
  }
  
  /* Melhorar legibilidade com sombras mais fortes */
  .drop-shadow-2xl {
    filter: drop-shadow(0 25px 25px rgb(0 0 0 / 0.8));
  }
  
  .drop-shadow-3xl {
    filter: drop-shadow(0 35px 35px rgb(0 0 0 / 0.9));
  }
  
  .shadow-3xl {
    box-shadow: 0 35px 60px -12px rgba(0, 0, 0, 0.8);
  }

  /* Otimizações específicas para mobile */
  @media (max-width: 768px) {
    #quem-sou {
      min-height: 70vh !important;
    }
    
    /* Desabilitar background-attachment: fixed no mobile para melhor performance */
    #bg-image-1,
    #bg-image-2,
    #bg-image-3 {
      background-attachment: scroll !important;
    }
    
    /* Reduzir animações no mobile para melhor performance */
    .animate-bounce {
      animation-duration: 2s;
    }
  }

  @media (max-width: 480px) {
    /* Ajustes para telas muito pequenas */
    #quem-sou {
      min-height: 65vh !important;
      padding-top: 3.5rem !important;
    }
  }

  @media (min-width: 769px) and (max-width: 1023px) {
    /* Ajustes para tablets */
    #quem-sou {
      min-height: 85vh !important;
    }
  }
</style>